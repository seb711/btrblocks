@PACKAGE_INIT@

include_directories(@GLOBAL_OUTPUT_PATH@")

include(CMakeFindDependencyMacro)
find_dependency(Threads)

find_dependency(croaring)
if (NOT croaring_FOUND)
  set(croaring_FOUND 0)
  set(croaring_NOT_FOUND_MESSAGE "croaring not found: ${croaring_NOT_FOUND_MESSAGE}")
endif ()

include("${CMAKE_CURRENT_LIST_DIR}/btrblocksTargets.cmake")

check_required_components(btrblocks)