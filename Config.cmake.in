@PACKAGE_INIT@

include_directories("@CROARING_INCLUDE_DIR@")

include(CMakeFindDependencyMacro)
find_dependency(Threads)

find_dependency(roaring)
if (NOT roaring_FOUND)
  set(roaring_FOUND 0)
  set(roaring_NOT_FOUND_MESSAGE "roaring not found: ${roaring_NOT_FOUND_MESSAGE}")
endif ()

include("${CMAKE_CURRENT_LIST_DIR}/btrblocksTargets.cmake")

check_required_components(btrblocks)